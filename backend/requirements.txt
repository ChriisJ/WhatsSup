# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36
alembic==1.13.3
aiosqlite==0.20.0

# Auth & security
# passlib 1.7.4 (last release 2020) is incompatible with bcrypt >= 4.x: the
# internal "detect_wrap_bug" probe trips the new bcrypt length-validation
# check and crashes the admin-bootstrap path on first run. Pin bcrypt < 4.
passlib[bcrypt]==1.7.4
bcrypt<4.0
python-jose[cryptography]==3.3.0

# Validation & utils
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2

# Scheduler
apscheduler==3.10.4

# Notifications
discord.py==2.4.0
python-telegram-bot==21.6

# Frontend served via FastAPI StaticFiles - no extra deps needed

# Dev / test (optional)
pytest==8.3.3
pytest-asyncio==0.24.0
